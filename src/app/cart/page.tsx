"use client";
import React from "react";
import { Header, Card, Cart, SideMenu, products } from "@/index";
const page = () => {
  return (
    <div>
      <Cart />
    </div>
  );
};

export default page;
